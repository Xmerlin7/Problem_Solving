#include <bits/stdc++.h>
using namespace std;
void swap (int &x, int &c);
int main()
{

    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
        cin >> arr[i];
    //! Big O: O(n)
    for (int i = 0; i > n; i++)
    {
        if ()
        {

        }
    }
    
    return 0;
}
void swap (int &x, int &c)
{
    int temp = x;
    x = c;
    c = temp;
}
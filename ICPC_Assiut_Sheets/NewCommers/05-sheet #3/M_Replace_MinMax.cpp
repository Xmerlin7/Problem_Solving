#include <bits/stdc++.h>
using namespace std;
int main()
{

    int maxx = numeric_limits<int>::max();
    int min = numeric_limits<int>::min();
    int size;
    cin >> size;
    int arr[size];
    for (int i = 0; i < size; i++)
    {
        cin >> arr[i];
    }

    while (true)
    {
    }
    cout << endl;
}
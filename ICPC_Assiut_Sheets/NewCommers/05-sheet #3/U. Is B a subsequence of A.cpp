#include <bits/stdc++.h>
using namespace std;
int main()
{
    int sizeA, sizeB;
    cin >> sizeA >> sizeB;
    long long arrA[sizeA], arrB[sizeB];
    for (int i = 0; i < sizeA; i++)
    {
        cin >> arrA[i];
    }
    for (int i = 0; i < sizeB; i++)
    {
        cin >> arrB[i];
    }
}
#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n, x, found = 0;
    cin >> n;

    int arr[n];
    for (int i = 0; i < n; i++)
        cin >> arr[i];
    //! Big O: O(n)
    for (int i = 0; i < n; i++)
    {
        if (arr[i] <= 10)
        
    }


    return 0;
}